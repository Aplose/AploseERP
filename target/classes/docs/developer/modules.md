# Développement de modules

## À venir

L’API d’extension par **modules** n’est pas encore disponible. Lorsqu’elle sera livrée, cette section sera complétée avec :

- Les points d’extension (hooks, événements)
- L’API interne exposée aux modules
- Des exemples de modules (structure, enregistrement, dépendances)

En attendant, les évolutions se font directement dans le code source du projet. Consultez la structure des packages et le code existant pour vous aligner sur les conventions.
